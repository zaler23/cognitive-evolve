from .base import PromptOperator


def build_operator() -> PromptOperator:
    return PromptOperator("lens_transplant", "immunology_topology_economics", "Reinterpret the problem through a distant disciplinary lens and emit a structurally different candidate.")
